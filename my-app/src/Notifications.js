import React from 'react';
import { Alert } from 'reactstrap';

const Noti = (props) => {
  return (
    <div>

      <Alert color="success">
        This is a success alert — check it out!
      </Alert>
    
    </div>
  );
};

export default Noti;
